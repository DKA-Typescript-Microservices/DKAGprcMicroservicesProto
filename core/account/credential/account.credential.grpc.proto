syntax = "proto3";

package credential;

import 'core/account/credential/account.credential.common.grpc.proto';

service Credential {
  rpc Authorization(common.AccountAuthRequest) returns (common.IAccount);
}

