syntax = "proto3";

package credential;

import 'account/credential/account.credential.common.grpc.proto';

service Credential {
  rpc Authorization(common.AccountAuthRequest) returns (stream common.IAccount);
}

